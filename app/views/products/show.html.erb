<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Show Products</title>
  <style type="text/css">
    body {
      margin-top: 2em;
    }
  </style>
</head>
<body>
  <div class="container">
  <h1>Product Details</h1>
    <div class="row">
      <div class="twelve columns">
        <p><b>Name:</b> <%= @product.name %></p>
        <p><b>Description:</b> <%= @product.description %></p>
        <p><b>Price:</b> <%= @product.pricing %></p>
      </div>
    </div>
    <a class="button" href="/products/<%=@product.id%>/edit">Edit</a>
    <a class="button" href="/">Go Back</a>
  </div>

  <div class="container">
    <div class="row">
      <div class ="six columns">
  <hr>
      <form action="/comment/<%=@product.id%>" method = "post">
      <input name="authenticity_token"
      value="<%= form_authenticity_token %>" type="hidden">
        <label for="commentBox">Post a Comment</label>
        <textarea name="text" class="u-full-width" id="commentBox"></textarea>
        <input class="button-primary" type="submit" value="Add Comment">
      </form>
  <hr>
        <% @comment.each { |obj| %>
          <p> <%= obj.comment %> </p>
        <% } %>
      </div>

  </div>
</body>
</html>